class Generator {
  generateSequence(a) {
    function nestedFunction(b) {
      return b + " Nested!";
    }

    const greetArrowMultiple = (greeting, name) => greeting + ", " + name + "!";

    return 3;
  }
}
